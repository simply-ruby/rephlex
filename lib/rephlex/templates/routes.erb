route do |r|
  # index
  r.root {}

  r.on Integer do |i|

  # show /<%= res_name %>/:id
  r.get do
  end

  # create /<%= res_name %>/:id
  r.post do
  end

  # update /<%= res_name %>/:id
  r.patch do
  end

  # delete /<%= res_name %>/:id
  r.delete do
  end
end
